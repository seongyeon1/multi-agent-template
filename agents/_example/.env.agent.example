# example_agent 고유 환경변수 — 전부 EXAMPLE_AGENT_ prefix.
# 공통 인프라값(LLM_GW_/OCR_/S3_/REDIS_/PII_ 등)은 루트 .env.common.example 참조.
# 실제 배포는 이 둘을 합쳐 .env 를 구성한다.

# API 바인딩
EXAMPLE_AGENT_API_HOST=127.0.0.1
EXAMPLE_AGENT_API_PORT=8000

# 기능 토글 — 기본 off(opt-in). 운영자가 점진 개방.
EXAMPLE_AGENT_NEW_PARSER_ENABLED=0
