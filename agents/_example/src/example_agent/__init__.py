"""example_agent — 새 에이전트의 기준(reference) 구현체.

이 패키지는 `agent_common`(공통) 위에 얹힌 한 에이전트의 최소 골격을 보여준다:
settings(prefix) · domain(순수) · application(use-case) · api(transport).
다른 에이전트를 import 하지 않는다 — 격리 규약.
"""
